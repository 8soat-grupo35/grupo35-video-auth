apiVersion: apps/v1
kind: Deployment
metadata:
  name: video-auth
spec:
  replicas: 2
  selector:
    matchLabels:
      app: video-auth
  template:
    metadata:
      labels:
        app: video-auth
    spec:
      containers:
        - name: video-auth
          image: video-auth:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: CLIENT_ID
              value: "16j9h9kmmlregfdcqr0fqldv6u"
            - name: CLIENT_SECRET
              value: "la42qrepmo3ja48cb98pjaqn8eojf1jgj0jjleqdugh52nrq76e"
            - name: REDIRECT_URL
              value: "https://d84l1y8p4kdic.cloudfront.net"
            - name: ISSUER_URL
              value: "https://cognito-idp.us-east-1.amazonaws.com/us-east-1_zECx5gApz"
